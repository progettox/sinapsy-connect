# Backend webhooks/events (optional)

Eventi possibili lato backend:
- moderation.updated
- project.status_changed
- notification.dispatch_requested

MVP: opzionale, puoi usare solo logica client + realtime.
